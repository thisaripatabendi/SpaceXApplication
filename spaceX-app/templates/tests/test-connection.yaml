apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spaceX-app.fullname" . }}-test-connection"
  labels:
{{ include "spaceX-app.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "spaceX-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
